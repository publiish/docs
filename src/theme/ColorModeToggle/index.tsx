import React from 'react';
import ThemeToggle from '@site/src/components/ThemeToggle';

// This is a wrapper component to replace the default ColorModeToggle with our custom one
export default function ColorModeToggle(): JSX.Element {
  return <ThemeToggle />;
} 